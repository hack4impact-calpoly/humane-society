import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1d4d71',
    },
    secondary: {
      main: '#4aa7ac',
      contrastText: '#ffffff',
    },
  },
});

export default theme;
